version: 2

models:
  - name: customers
    description: "This table contains information on when a customer made an order with us"
    columns:
      - name: customer_id
        description: "unique identifier, column has to be unique and not null"
        tests:
            - unique
            - not_null

      - name: first_order_date
        description: ""

      - name: most_recent_order_date
        description: ""

      - name: number_of_orders
        description: ""